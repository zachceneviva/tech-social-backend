// Imports
const router = require('express').Router()
const ctrl = require('../controllers')

// Index
router.get('/', )

// Create route
router.post('/', )

// Update Route
router.put('/:id', )

// Delete Route
router.delete('/:id')

// Export
module.exports = router